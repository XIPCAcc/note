LLDB调试方法

在底部Debug console 输入命令查看内存内容

memory read --size 8 --format x --count 32 0x00007ffff7bfe758

从反汇编结果来看

接收端接收到uipi后，会构造一个168字节的栈帧，其中 栈最顶部为 vector，-8 为 rip，-16 为 rflags，-24 为rsp。

```
+-----------------+
|   额外的8字节    |  ← 内核压入，需要清理
+-----------------+
|  UintrFrame结构  |  ← 包含rip, rflags, rsp
+-----------------+
|  被中断程序栈  |
```

uiret会从栈中弹出对应内容然后返回。

使用rust构造的ui handler会有什么问题。

fn client_ui_handler() {
        unsafe {
        asm!(
            "pop rax",
            "uiret", 
        options(noreturn)); // noreturn 的作用是不生成ret指令
    }
}

会生成

pushq %rax  // 这条指令是编译器自动生成的
popq %rax   // 主动加上这条指令，对齐栈
uiret

查看栈的内容，确实和C写的handler一样，但是uiret 返回就是会segmentation fault。

因为uiret默认栈顶部是rip，然后弹出rip返回。所以需要手动 add rsp, 8，将栈顶部的vector去掉。


变成


fn client_ui_handler() {
        unsafe {
        asm!(
            "pop rax",
            "add rsp, 8",
            "uiret", 
        options(noreturn));
    }
}

相当于手动维护栈结构。


但是由此带来的问题是handler 中不能调用其他函数，因为rust编译器是caller方负责将参数压入callee的栈中，因为会更改rsp指针。但是手动插入noreturn的汇编，编译器就不会生成callee函数返回时维护栈指针的代码。就算不用noretrun的汇编，维护栈指针的代码也会被放到 uiret汇编指令后面而无法执行。