# 用户态中断和epoll异步等待的性能对比

如果选择epollfd 作为传递共享内存ready的标志，那么收发双发都必须经过一次系统调用（sys write 或者sys read）。
用户态中断方式则不需要，只需要一次用户态跳转设置标志位即可。

但是这种开销在协程调度的开销量级前面会被磨平。所谓协程调度开销是指异步等待标志位未就绪后，协程需要等待IO driver唤醒并重新按照FIFO方式调度的时间。

对于epoll而言，是通过IO driver 调用epoll_wait的时间、协程唤醒的时间，加上协程排队运行的时间。

对于用户态中断，IO driver检测标志位的时间、协程唤醒的时间，加上协程排队运行的时间。

# IO driver

之前的做法是直接在原本的IO driver中添加检测用户态中断的逻辑，但是由此带来的问题是中断路由的问题必须通过self-pipe将用户态中断转换为epoll event的方式解决。

由此带来的问题是低负载状态下用户态中断唤醒的时间较长。

解决办法是单独实现一个用户态中断IO driver，并且该driver只能由注册用户态中断的线程获取。

在tokio多线程运行时中，主线程只负责驱动main函数的运行，不参与工作线程的任务窃取也不运行 tokio::spawn的任务，大部分时间处于park condv状态，因为选择把用户态中断注册在主线程可以避免频繁中断打断计算任务带来的开销。

因为改造的思路是让主线程运行的IO driver检测用户态中断标志位或者等待用户态中断唤醒。