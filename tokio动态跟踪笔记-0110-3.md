tokio signal 处理方式

创建一对本地 socket（UnixStream::pair），类似一个匿名管道：

```rust
let (receiver, sender) = UnixStream::pair().expect("failed to create UnixStream");
```
receiver：读端，注册到 mio 的 reactor（epoll）里；sender：写端，留给信号处理函数使用。
注册全局信号处理回调：在 tokio/src/signal/unix.rs 里，通过 signal_hook_registry::register(signal, move || action(globals, signal)) 注册一个 OS 信号处理函数 action()：

```rust
/// 本模块注册的所有信号的全局信号处理器。
///
/// 该信号处理器的主要目的是：
///
/// 1. 标记特定信号已被接收（例如，存储一个原子标志）
/// 2. 通过向管道写入一个字节来唤醒驱动程序
///
/// 这两个操作都应该是异步信号安全的。
fn action(globals: &'static Globals, signal: libc::c_int) {
    globals.record_event(signal as EventId); // 标记该信号触发

    // 用 sender 往 socket 写一个字节，唤醒 epoll
    let mut sender = &globals.sender;
    let _ = sender.write(&[1]);
}
```

回调里做的只有两件事：设置原子标志 + 往 pipe 写一个字节，都属于 async-signal-safe 操作。
把 receiver 注册进 mio（epoll）：
Tokio 的底层 runtime 用 mio 作为 reactor；mio 再将 receiver 注册到 epoll 的「可读事件」集合。

事件循环中 epoll_wait：
runtime 的 I/O 驱动 loop：
调用 epoll_wait 等待所有受管理 fd（socket、pipe 等）的可读/可写；
当有信号来时，action() 往 sender 写字节，导致 receiver 上出现可读事件；
epoll 返回「某 fd 可读」，mio 处理到这个 UnixStream：
先把管道里的字节读出（清理）；
然后根据全局状态 globals.record_event() 得知哪个信号来过；
对该信号对应的 EventInfo 触发一次 broadcast（watch::Sender<()>），唤醒所有 Signal 监听者。
上层 Future 如何被唤醒？

上层 tokio::signal::unix::signal(SignalKind)：
```rust
pub fn signal(kind: SignalKind) -> io::Result<Signal> {
    let handle = scheduler::Handle::current();
    let rx = signal_with_handle(kind, handle.driver().signal())?;
    Ok(Signal { inner: RxFuture::new(rx) })
}
globals().register_listener(kind.0 as EventId) 返回一个 watch::Receiver<()>；
```
Signal 内部就是在这个 watch::Receiver 上 recv().await；
当 epoll 检测到 pipe 可读并处理后，会对对应 Event 的 watch::Sender 发送 ()；
Signal Future 被唤醒，.recv().await 返回一次信号事件。

# 可以用用signal 唤醒任务吗

在现代异步/多线程框架里，一般不会“直接用信号”来驱动业务或事件循环，而是先把信号转换成普通 I/O 事件（fd 可读），再交给 epoll / io_uring / runtime 去统一处理。
原因主要有这几类：



1 信号是“异步中断模型”，和事件驱动/async 模型冲突
信号：随时打断当前线程，强制执行 handler；
可以在 任意时间点 打断当前正在执行的线程；
把控制权切到 signal handler 上；
handler 结束后再返回被打断的位置。


事件驱动（epoll/io_uring/async）希望的是：
代码在 明确的时间点 调用 epoll_wait / await；
当有事件发生时再被有序地唤醒；
唤醒点可预测、可推理。

冲突点：
信号可以在任何时间点打断你的代码，包括：
正在持锁；
正在做内存分配；
正在执行非线程安全代码；
在 handler 里，你只能调用一小撮 async-signal-safe 函数（write、_exit 等），
不能做正常业务逻辑，更不可能 await

2 多线程环境下，信号分发不可控

在多线程程序里：
对“整个进程”发送信号时，内核会在 未屏蔽该信号的线程中任选一个 递送；
除非你做一大堆 pthread_sigmask、专门信号线程、sigwait/signalfd 的配置，否则：
你不知道 到底是哪个线程的 read(pipe) 会被打断；
甚至可能打断一个完全不相关的阻塞系统调用（比如在别的 fd 上）。

很难保证“总是由某个特定线程来处理这个信号”。

如果你有 N 个工作线程：
某个线程在处理关键逻辑，持有共享锁；
某个信号突然被送到它那里，handler 里面一旦碰到不安全操作，就各种死锁、数据竞争。
要避免这些问题，必须：
在所有普通线程上调用 pthread_sigmask(SIG_BLOCK, ...) 屏蔽这些信号；
再创建一个专门的“信号线程”，用 sigwait / signalfd 等同步地接收信号。
这套东西一旦你自己来写，很快会变复杂、容易出 bug。
所以现代 runtime 统一自己做这件事，对用户只暴露一个安全的 async 接口。


3 信号语义粗糙、容易丢信息
信号只能携带极少的信息（基本就是 signal number，加点有限的 siginfo）；
多个相同信号短时间到达可能合并成一次，中间次数信息会丢失；

无法像 socket 那样“每个事件都有明确的数据边界”。
对于高并发、精确事件驱动场景：
你需要的是“每个事件都能独立处理、不会丢、可排队”的机制；
这一点上，“读一个 fd / 从完成队列里拿一个 CQE”要比“收一个信号”可控得多。

Linux 下pipe 阻塞的进程可以设计用信号唤醒吗？为什么不用信号?
从机制上讲可行：
被 read(pipe) 阻塞的进程，确实可以被信号“唤醒”（严格说是：被信号打断系统调用，从阻塞中返回，通常返回 EINTR）。
工程实践上：
高质量程序 / 异步框架里，一般不会刻意用信号来作为 pipe 阻塞的“唤醒手段”，而是让：
写端写数据 → 内核通过等待队列唤醒阻塞在 read/epoll 的进程；
或者把信号先转成 fd 事件（signalfd / self-pipe），再用 epoll/io_uring 统一处理。
原因核心在于：信号是异步中断，语义粗糙；pipe/epoll 是事件驱动，语义精确可控。


异步/事件驱动模型的特点：

所有任务在明确的点（await/poll）让出控制权；
唤醒顺序由调度器和事件循环管理；
调度顺序、状态转移都是可推理的。
如果你用信号直接“抢占式”唤醒：

handler 可能在持锁时被调用 → 死锁风险；
handler 中不能安全调用大部分库函数（不 async-signal-safe）；
你无法在 handler 里安全地访问 async runtime 内部的数据结构（比如任务队列、waker 列表）；
甚至都不确定信号会打到哪个线程（进程级信号在未屏蔽线程中任选一个）。
所以，对异步运行时来说：

直接把“唤醒逻辑”放在 signal handler 里，是灾难性的设计，
它会破坏“任务状态机 + waker + Poll/Ready”这整套抽象。

虽然异步框架确实会有实时性的问题
但是“抢占实时唤醒”带来的是不可控，而不是更快
信号“更快”的代价是：
不可控的时机：打断点随机；
不可控的线程：哪个线程收到信号不一定；
不可控的上下文：也许在 malloc 中、也许在用户锁里、也许在 runtime 正改调度队列。
相比之下，异步运行时有限定的“明确唤醒点”：

只在事件循环、await、poll 的时刻变更任务状态；
所有状态变化都集中在少数几个地方，更易推理和保证线程安全；
这部分代码可以做重度优化（无锁结构、批量处理等），最终整体延迟也很小。

所以从工程实践看：
“可控 + 稳定”通常比“理论上的多一点抢占即时性”更重要。

对于绝大多数你接触到的场景（网络服务、后端、工具）：
异步 runtime + epoll/io_uring + signalfd/self-pipe 就已经足够“即时”；
关键不是“再去加一层信号抢占”，而是：
不要在单个任务里写长时间的 CPU 死循环；
把大任务拆碎，多次 await，让出执行权；
I/O 操作都走 runtime 的异步接口，避免阻塞整个线程；
runtime 线程不要被别的重 CPU 任务长期占用（如重编解码、压缩），这些应该丢到专门的 CPU 线程池。

current_thread 调度器会按一个固定上限（event_interval，默认 61）批量执行一小段任务；
每批执行完后都会通过 park_yield 主动让 driver 跑一轮 I/O/timer（底层就是 mio::Poll::poll）；
只有在当前没有任何就绪任务时，才会调用真正会阻塞的 park（等待 I/O 事件唤醒）。