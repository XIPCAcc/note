# 守护进程：通过串口收数据块

使用Rust编写一个守护进程，能够通过串口读取矩阵数据计算完成后发送回去。

不使用第三方库，只能使用Rust标准库。

串口读写使用的是 sys:read() 和 sys::write()
```c
    pub fn read(fd: c_int, buf: *mut c_void, count: usize) -> isize;
    pub fn write(fd: c_int, buf: *const c_void, count: usize) -> isize;
```

阻塞式守护进程和非阻塞式的守护进程。

（1）阻塞式的守护进程

串口的配置模式决定了 sys:read() 是否会阻塞。
```
    // ── 读取模式（VMIN + VTIME 组合语义）──
    //   VMIN=1, VTIME=0 → 阻塞等待：read() 永远阻塞，直到收到至少 1 字节
    //   VMIN=0, VTIME=0 → 非阻塞：read() 立即返回，有数据返回数据，无数据返回 0
    //   VMIN=0, VTIME=N → 带超时：read() 最多阻塞 N×100ms，超时返回 0
```

阻塞模式下的守护进程会在 sys:read() 无数据时候挂起并让出处理器，内核收到串口中断唤醒进程。

内核通过中断唤醒阻塞的进程

```
1. 进程调用 read(fd, buf, count)
   → 内核发现串口缓冲区没数据
   → 把进程状态设为 TASK_INTERRUPTIBLE（可中断睡眠）
   → 把进程加入串口设备的等待队列
   → 主动让出 CPU，调度其他进程运行

2. 串口硬件收到数据
   → 触发硬件中断（IRQ）
   → CPU 跳转到串口驱动中断处理函数
   → 驱动把数据写入接收缓冲区
   → 驱动调用 wake_up_interruptible(&port->wait_queue)
   → 内核把进程状态改为 TASK_RUNNING
   → 下次调度时进程恢复运行，read() 返回数据
```

（2）非阻塞的守护进程

sys:read() 直接返回，采取轮询方式读取串口数据，或者通过IO多路复用 poll等待。

如果通过poll等待，则进程阻塞在 poll_wait 系统调用，进程的唤醒方式和阻塞式的流程大致相同。考虑到整个守护进程只有 串口一个fd，那么IO多路复用和阻塞式的守护进程并无太大的区别。

轮询方式读取串口 https://github.com/XIPCAcc/UART/commit/ece807758e1a9323cf31a90f8d31a29175e8b852


