运行方式

```
# 启动服务器
cargo run -- --server 10

# 在另一个终端启动客户端
cargo run -- --client 10
```


# 方案一

设计两个标志，一个pending给future poll使用，一个Interrupt_receive给callback和唤醒使用。

在turn()中负责检测是否接受到中断然后唤醒。

通过采用 new_current_thread 单线程运行，同时设计一个定时的任务确保worker不会睡眠

定时任务tokio::task::yield_now().await; 不会像 sleep 一样真的等待一段时间，只是让出一个调度轮次，通常很快就会被再次调度，所以队列中就会始终有任务


## 不同批次延迟时间对比

| 批次 | Message Count | 平均时间 (µs) | 最小时间 (µs) | 最大时间 (µs) |
|------|---------------|---------------|---------------|---------------|
| 批次 1 | 1000 | 90.972 | 86.201 | 1429.588 |
| 批次 2 | 100 | 66.752 | 46.913 | 143.498 |
| 批次 3 | 10 | 66.773 | 32.096 | 226.180 |
| 批次 4 | 1 | 282.290 | 282.290 | 282.290 |

这个时间和interval有关系，因为队列是始终有任务的，只有运行了interval次以后才会进入turn()。

同时在定时任务中

```rust
tokio::spawn(async {
    loop {
        for _ in 0..2000 {
            std::hint::spin_loop();
        }
        tokio::task::yield_now().await;
    }
});
```

性能对比之前的明显增大很多
消息数量 | tokio signal (平均/最小/最大) | compio signal unix (平均/最小/最大) | compio signal linux (平均/最小/最大) | C signal (平均/最小/最大) | C uintr (平均/最小/最大) | Rust uintr (平均/最小/最大) |
-- | -- | -- | -- | -- | -- | --
1 | 301 / 301 / 301 | 672 / 672 / 672 | 199 / 199 / 340 | 44.0 / 44.0 / 44.0 | 0.768 / 0.768 / 0.768 | 1.3 / 1.3 / 1.3
10 | 67 / 21 / 441 | 153 / 16 / 639 | 154 / 27 / 330 | 48.2 / 8.96 / 290 | 2.56 / 2.3 / 2.8 | 1.7 / 1.56 / 2.38
100 | 92 / 42 / 887 | 55 / 25 / 443 | 60 / 16 / 538 | 19.8 / 10.75 / 159.7 | 2.6 / 2.56 / 5.38 | 1.27 / 1.07 / 2.48
1000 | 52 / 33 / 1206 | 26 / 20 / 1083 | 47 / 31 / 592 | 8 / 6.9 / 88.3 | 1.24 / 1.02 / 6.14 | 0.78 / 0.74 / 4.17


原因分析，第一 定时任务的开销导致，毕竟及时支持优先级也是无法抢占的，而且还要等到interval次运行才会进入turn()。
第二，测试程序中异步信号的的发送方发送信号后进入等待的周期和接收方处理信号的周期存在重叠（发送方直接就到epoll wait睡眠等着了）。多次收发以流水的方式在一定程度上降低的开销。

但是异步中断发送完信号，又调度到另一个任务，所以收发双方就完全是串行了。

# 方案二

不再等待进入turn()唤醒，直接在中断处理函数中wake。问题是在中断处理函数中wake()会导致 RefCell Core的竞争问题。
thread 'main' (2877964) panicked at tokio/tokio/src/runtime/scheduler/current_thread/mod.rs:662:40:
RefCell already borrowed

实际运行就是会不定期死锁卡住。

```rust
// 可以运行的次数又和这个定时任务的调度频率有关

tokio::spawn(async {
    loop {
        for _ in 0..1000 {
            std::hint::spin_loop();
        }
        tokio::task::yield_now().await;
    }
});
```

## 不同批次延迟时间对比

| 批次 | Message Count | 平均时间 (µs) | 最小时间 (µs) | 最大时间 (µs) |
|------|---------------|---------------|---------------|---------------|
| 批次 1 | 1 | 143.246 | 143.246 | 143.246 |
| 批次 2 | 10 | 36.797 | 3.410 | 73.745 |

# 方案三

使用signal类似的方式注册epoll，测试一下性能。（依然需要定时任务，因为用户态中断无法唤醒进程）

从内核实现角度看，signal 之所以能打断 epoll_wait 并让其立即返回，关键在于三点：
epoll_wait 的实现把当前线程置为 可被信号打断的睡眠状态（TASK_INTERRUPTIBLE）​；
内核在睡眠/调度点会检查当前线程是否有 待处理信号（signal_pending(current)）​；
一旦发现有信号挂起，就让等待提前结束，并让 epoll_wait 返回 -EINTR。


## 不同批次性能测试结果对比

使用用户态中断加epoll，同时增加一个定时任务

```rust
    tokio::spawn(async {
        loop {
            tokio::task::yield_now().await;
        }
    });
```

| 批次 | 消息数量 | 总耗时 (ms) | 平均延迟 (µs) | 最小延迟 (µs) | 最大延迟 (µs) | 消息速率 (msg/s) |
|------|----------|-------------|---------------|---------------|---------------|------------------|
| 1 | 1000 | 23.074 | 22.963 | 17.597 | 473.962 | 43,339 |
| 2 | 100 | 3.204 | 31.759 | 16.658 | 814.521 | 31,205 |
| 3 | 10 | 0.818 | 81.533 | 20.678 | 375.541 | 12,218 |
| 4 | 1 | 0.581 | 578.399 | 578.399 | 578.399 | 1,721 |

# 方案四

使用uintr wait替代epoll_wait

并且使用一个中断标志完全的替代epoll，直接检测这个中断标志是否已经设置，如果没有设置，那么才epoll_wait。