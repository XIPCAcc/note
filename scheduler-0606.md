# 关于引入支持多线程调度的调度器的思考

tokio中的多线程调度机制只支持由一个working thread等待外部事件的方式唤醒协程。

目前缺乏一个调度器，能够接受多种不同的事件源并以全局视角唤醒和调度协程。（比如中断）

而且tokio中的调度器是每个working thread都分别执行自己的调度器执行流，只负责调度自己的协程。

## 问题分析

在 gateway-gRPC 项目中尝试利用用户态中断来提升性能，目标是让中断处理在用户态完成，避免进内核。但遇到了系统性的调度问题。

```
中断到达 → UI handler 设置标志位 → 等待中的协程被唤醒 → 处理请求
```


| 方案 | 做法 | 问题 |
|---|---|---|
| 方案一：Self-pipe | 中断处理中通过 `pipe write` 把中断转为 epoll 事件，唤醒等待协程 | 每次唤醒都要进一次内核（`sys_write`），把用户态中断的优势抵消了 |
| 方案二：直接用 uint_wait | 用 `uint_wait` 替换 `epoll_wait`，中断处理中只设标志位不写 pipe | 多线程下死锁：用户态中断只路由到注册了中断的 worker thread，但空闲后去跑 `epoll_wait` 的可能是另一个线程——中断和等待不在同一个线程上 |
| 方案三：分离阻塞/非阻塞线程池 | 单独一个核跑阻塞式协程（loop 轮询 uint_wait），其他核跑非阻塞协程 | 1) tokio 内部阻塞线程池唤醒非阻塞协程时仍会产生系统调用；2) 中断风暴导致在进系统调用前所有中断已发完，白等超时；3) 偶尔仍死锁 |

具体来说，tokio 调度器有三个致命缺陷：

1. per-worker 独立调度：每个 worker thread 只管自己的任务队列，没有全局视角
2. 事件通知与执行流绑定：中断通知到某个特定线程，但执行可能在另一个线程上，导致"通知不到"或"死锁"
3. 只有一个线程负责接收外部事件：tokio 依赖一个"主"线程接收 IO 事件，然后唤醒其他 worker，无法让所有线程都能响应外部事件


## vsched2

vsched2 要解决的是：在用户态多核环境下，让调度器拥有全局视角，使得事件通知不再绑定特定执行流，而是以地址空间为单位进行调度。

| tokio 的缺陷 | vsched2 的解决方案 |
|---|---|
| 每个 worker 独立调度，无全局视角 | 全局调度器：所有核心共享一个调度器，统一决策 |
| 中断通知绑定特定线程 | 事件源抽象：中断可以注册为事件源，调度器在全局视角下选择最优核心响应 |
| 只有一个线程能接收事件 | 任意核心都能响应：空闲核心自动从调度器取任务，事件到达时正在运行的核心先响应 |
| 工作线程进入 idle 后无法被合理唤醒 | 核间中断唤醒：低功耗核心通过核间中断唤醒后直接去调度器取任务 |


1. 替换 tokio 的调度器：用 vsched2 的全局多核调度逻辑替代 tokio 的 per-worker 独立调度，解决"事件到达但通知不到正确线程"这个根本问题

2. 先不考虑 vDSO：第一步只需要把 vsched2 的调度算法和事件源架构搬到用户态进程中，不涉及 vDSO 的内核-用户态共享

3. 统一事件源：让用户态中断和其他事件源（IO、定时器等）在同一个调度框架下工作，调度器在全局视角下选择最高优先级任务

4. 目标效果：当中断到达时，调度器能够主动决定由哪个核心响应——如果所有核心都在忙，正在运行的核心先响应；如果某个核心空闲，唤醒它去响应。不再出现"中断送到了线程 A，但线程 B 在等事件"的死

## 可行性分析


| 障碍 | 说明 |
|---|---|
| Task trait 不兼容 | vsched2 有自己的 `Task` trait（含 `is_coroutine()`、`poll()`、`context_restore()` 等），与 tokio 的 `Future` trait 完全不同 |
| Waker 机制不同 | tokio 用 `std::task::Waker` 唤醒任务，vsched2 用 trap 驱动 + 直接上下文切换，无 Waker 概念 |
| 调度循环不同 | vsched2 的调度入口是 `trap_entry`（陷阱/中断驱动），tokio 是协作式 `.await` 让出 |
| 事件源依赖 vtable | `event_source.rs` 的 vtable 通过 `trait_interface!` 宏生成，绑定 vDSO 基础设施 |
| 栈管理不同 | vsched2 管理独立的内核栈池 + 栈切换跳板，tokio 的任务跑在线程栈上 |
| tokio 无法替换调度器*| tokio 的调度器与 runtime 深度耦合，没有公开的"替换调度器"接口 |


用 vsched2 的设计思想，在 tokio 之外构建一个新的调度层：

```
┌──────────────────────────────────────┐
│      vsched2 风格调度层（新写）        │
│  ┌──────────┐  ┌──────────┐         │
│  │ 就绪队列  │  │ UINT源   │  ...    │  ← 事件源抽象
│  │ (优先级)  │  │ (中断)   │         │
│  └────┬─────┘  └────┬─────┘         │
│       └──────┬──────┘               │
│         全局 pop_task()              │
│       (跨核心优先级比较)              │
└──────────────┬───────────────────────┘
               │ 分发任务
┌──────────────┴───────────────────────┐
│         tokio runtime                │
│   worker 0    worker 1    worker N   │
│   (只负责执行，不负责调度决策)          │
└──────────────────────────────────────┘
```

核心思路：
1. 上层调度器（借鉴 vsched2 的事件源 + 全局优先级）负责调度决策
2. 下层 tokio runtime 只负责执行任务
3. 用户态中断作为事件源直接接入上层调度器，不再经过 self-pipe → epoll 的迂回路径
4. 调度器知道所有核心的状态，能决定让哪个核心响应中断
