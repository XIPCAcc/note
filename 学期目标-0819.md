
## 理想中的IPC伪代码

1. 最简单的单向通道收发
```rust
spawn () {
    loop {
        Sender.write(buf).await()?
    }
}

spawn() {
    loop {
        buf = Receiver.read().await()?
    }
}
```

2. 单向双通道收发

```rust
spawn() {
    loop {
        Sender1.write(buf).await?
        res = Receiver2.read().await?
    }
}

spawn() {
    loop {
        buf = Receiver1.read().await?
        res = Compute(buf)
        Receiver2.write(res).await?
    }
}
```

3. 双向单通道收发

```rust
spawn() {
    loop {
        client.write(buf).await?
        res = client.read().await?
    }
}

spawn() {
    loop {
        buf = server.read().await?
        res = Compute(buf)
        server.write(res).await?
    }
}
```

4. 双向多通道收发

```rust
main() {
    loop {
        spawn {
            let (client, _) = connect().await?;
            client.write(buf).await?
            res = client.read().await?
        }
    }
}

main() {
    loop {
        let (server, _) = listener.accept().await?;
        spawn {
            buf = server.read().await?
            res = Compute(buf)
            server.write(res).await?
        }
    }
}
```

 
## 之前遇到的问题

### 问题一：等待用户态中断的任务waker应该存放在哪里？

通过全局变量保存对应的任务的waker或者wakerList。

### 问题二：在tokio中引入用户态中断后，为什么不能在用户态中断上下文调用wake()？

因为调用wake()涉及到入就绪队列操作，tokio的就绪队列无法实现原子入队。用户态中断到达的时机可能会打断原本正在入队的操作，从而导致panic。

Embassy的就绪队列出入队是原子操作，因此可以在中断上下文中直接调用wake()。

### 问题三：对于tokio而言，能否通过只在中断上下文设置标志表明收到用户态中断，然后再在Reactor中检查中断标志，从而调用wake唤醒任务？

单核场景下可行，但是在多核场景下，Reactor协程会随机的运行或者阻塞在任意一个worker线程之中。而用户态中断只会路由给一开始注册中断的worker线程，这就导致用户态中断无法唤醒Reactor协程阻塞的worker线程，从而导致死锁。

### 问题四：竞态问题如何解决？

竞态问题就是检查就绪队列是否为空然后决定是否要进入休眠状态的期间，可能会发生用户态中断的问题。

基于epoll或者Iouring的运行时不存在竞态问题，因为都是同步进入内核检查事件是否发生的。

Embassy的做法则是通过CriticalSection 关中断的方式。但是需要配合一些特定的原子休眠和唤醒指令才能觉接这个问题。

基于用户态中断的时候，虽然也有CriticalSection 关中断，但是x86没有原子休眠和唤醒指令，开中断后到真正进入休眠的一段时间仍然处于竞态，没办法彻底解决，目前只能通过尽可能缩短竞态窗口的方式降低出错的概率。

### 问题五：多核编程模型应该采用什么方式？

运行时按多核模式分类

1. 多核分区型（Thread-per-core / Partitioned scheduling）
每核一个执行器 + 独立队列，任务绑定核，永不迁移
embassy（executor-per-core）
glommio / monoio（Linux io_uring）

对于这种类型的运行时，需要应用层变成自行设计多核的任务分配，所以应用层的程序比较难写。
但是底层运行时实现起来比较简单，需要支持的是动态投递协程到另一运行时的spwan接口。

多核模式下的编程模型伪代码。
```rust
fn main() {
    // core0：IO 核 —— 收请求、回响应
    spawn_executor_on_core(0, |spawner0| {
        spawner0.spawn(ipc_receiver());
    });

    // core1：计算核 —— 常驻计算 worker
    spawn_executor_on_core(1, |spawner1| {
        spawner1.spawn(compute_worker());
    });

    // ② 把 core1 的"投递句柄"共享给 core0（启动期一次性）
    //    之后 core0 就能显式把任务投到 core1
    set_core1_spawner(get_spawner_of_core(1));
}

static REQ:   Channel<Request, 16> = Channel::new();   // core0 → core1
static RESP:  Channel<Response, 16> = Channel::new();  // core1 → core0

// ── core0 任务：接收 + 投递 + 回响应 ──
async fn ipc_receiver() {
    loop {
        // 等外部请求（socket / 中断 / 信号）
        let req = recv_ipc().await;

        // 方式 A：投递给 core1 上的常驻 worker（推荐，数据走 channel）
        // REQ.send(req).await;                 // 挂起直到 core1 有空位

        // 方式 B：动态在 core1 上 spawn 一个协程（一次性，投完固定）
        spawn_into(CORE_1, async move { ... });

        // 等 core1 算完，把结果写回发送方
        let resp = RESP.recv().await;
        send_ipc(resp).await;
    }
}

// ── core1 任务：常驻计算 worker ──
async fn compute_worker() {
    loop {
        let req = REQ.recv().await;          // 在 core1 上等数据（挂起）
        let result = compute(req);           // 计算发生在 core1
        RESP.send(result).await;             // 回传给 core0
    }
}

```

2. 多核共享型（Work-stealing / Global scheduling）
执行器共享任务池，任务可在核间迁移，动态负载均衡
tokio multi_thread / smol / async-std / futures ThreadPool

对于这种类型的运行时，单核和多核运行时对于应用层而言是透明的，所以应用层的程序都长一样，简洁优雅。
但是要求底层运行时负责队列任务的调度和迁移，运行时实现起来复杂。

## 学期计划

1. 基于单核用户态中断运行时，实现最简单的单向通道收发。
    1.1 单缓冲区和多缓冲区设计
    1.2 对比传统IPC，比如tokio的异步PIPE

2. 拓展为多核用户态中断运行时，采用Thread-per-core方式。

3. 依次实现单向双通道收发、双向单通道收发、双向多通道收发

4. 拓展串口，使硬件支持用户态中断