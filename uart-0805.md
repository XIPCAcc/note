# 关于竞态问题的处理

```rust
            unsafe {
                crate::uintr::syscall::clui();
                if !self.queue.is_empty() {
                    crate::uintr::syscall::stui();
                    continue;
                }

                crate::uintr::syscall::stui();
                crate::uintr::syscall::epoll_wait(0, u64::MAX);
            }
```

首先，stui指令并不保证后一条指令执行的原子性，一般开中断会，下一条指令立刻会检测之前是否有用户态中断挂起。

其次，进入epoll_wait （任何阻塞系统调用都一样）后，只有执行了schedule 切换到其他进程，内核的用户态中断处理程序才会执行唤醒操作。执行schedule前收到用户态中断的，此时默认执行的是不含唤醒功能的内核用户态中断处理程序，也会造成竞态。

## 如何尝试解决竞态问题

1. 进入系统调用的期间完全关闭用户态中断的投递，即stui指令放到系统调用返回后再执行。
2. 进入系统调用后，依然存在的竞态使用检查进行兜底。

```
1. syscall 层检查，schedule前检查 puir  (几十条指令)，仅 uintr_wait 实现
2. __schedule() signal_pending_state() 旁 已 pending 时改回 RUNNING，不 deactivate_task ，直接返回 所有可中断睡眠路径 （uintr_wait / epoll / futex / sigsuspend / nanosleep 等）
```