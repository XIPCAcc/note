1. 首先就是 epoll_wait 的实现，内核替换了很多次，都没有成功，其中及此次增加了printk 都无法正常 使用ssh🔗，一颗下单了串口、
   还有的是能够启动（uname -r 确定是6.0.0+）但是运行uintr_RUST测试都不成功，内核也抓不到打印信息（dmesg -w |grep epoll）

2. 今天发现之前使用 定时任务的计时单位弄错了，之前是5us，还以为是5ms。现在改成了3000us，初步效果感觉并没有很好。

3. 增大了并发数到8192，看起来uintr的表现能够好一些。但是整体都比定并发效果差。

4. 但是修改echo的计算延迟为10000us，而且是占用CPU的，导致uintr性能直线下降，反而是对epoll没影响，

5. wrk和echo不用担心，所有shm都走一条路径，其他的是给tcp，uds用的。

6. 先尝试中断合并