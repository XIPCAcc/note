# 串口收发的系统调用改用户态中断

## 进kernel

(1) 阻塞式 sys:read

进程依旧阻塞在 sys:read()。串口收到数据后，会执行串口驱动的中断处理程序，将原本会执行的wake_up_interruptable 改成内核向进程发送用户态中断。内核的用户态中断处理逻辑会负责调用 wake_up_interruptable。这个思路用户态中断显得有些多余。

(2) 非阻塞 sys:read

执行一个 uintr_wait 等待用户态中断，进程唤醒后读取串口数据。串口驱动的中断处理程序收到数据后向进程发送用户态中断，唤醒进程执行 sys:read 读取数据。

## kernel by pass

通过ioperm或者mmap将UART的寄存器映射到用户态，然后由用户态程序直接读取串口数据。

更改串口驱动的逻辑，去除驱动中读取寄存器的逻辑，改成发送用户态中断。

用户态程序需要注册用户态中断，然后在用户态中断处理程序中读取寄存器的值存入缓冲区。


## 一些问题

### 驱动如何向用户态程序发送用户态中断

方法一：驱动直接调用用户态中断相关的系统调用。需要解决用户态接收方的中断fd如何传递给驱动的问题

方法二：驱动直接写接收方 task struct 中的用户态中断相关数据结构，比如UPID等。需要考虑驱动如何获取到进程task struct 的问题。

方法三：参考之前SPDK 论文拓展用户态中断的方法，设备的中断号直接映射到内核用户态中断号，然后串口中断触发后直接执行内核用户态中的处理程序。目前还不知道内核是如何二次发送用户态中断给用户态程序的。

Q：当用户程序处于运行状态的时候，串口的中断是否能够配置成直接给用户态中断处理程序发送，不执行内核态中断中断处理程序，直接在用户态中断处理程序中执行。

当前的Intel示例程序能实现，是因为有一个同一台机器的发送方进程已经通过sendupip更改了接收方的UPID，PIR等标志位。如果需要跨机器运行的话，接收方必须通过内核的中断处理程序设置。

### 主程序应该如何修改

主程序暂时采用轮询方式读取缓冲区。

但是目前需要采用一个系统调用，比如uintr_wait()?阻塞等待。

导致的问题是有一个窗口期的问题

```
ui_handler () {
    XXXX
    uiret
}

if no uintr  // 判断的时候数据未就绪

    // 突然收到用户态中断，但是依然会进入阻塞等待系统调用，阻塞期间没有后续的用户态中断

    push eax
    push ebx
    syscall
    uintr_wait()?

end
else 
  

```


